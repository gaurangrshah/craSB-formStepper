import React from 'react'
import { useInput } from './utils/useInput'
import { useSteps } from './utils/useSteps'
const FormWrap = ({ inputs, children }) => {


  return (
    <form>

    </form>
  )
}

export default FormWrap
